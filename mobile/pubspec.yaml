name: aividcatalog18
description: "AIVidCatalog18 — Premium AI-Generated Video Catalog (Flutter Mobile App)"
publish_to: 'none'
version: 1.0.0+1

environment:
  sdk: '>=3.2.0 <4.0.0'

dependencies:
  flutter:
    sdk: flutter
  flutter_localizations:
    sdk: flutter

  # HTTP & API
  dio: ^5.4.0
  
  # State management
  provider: ^6.1.1
  
  # Local storage (token, settings)
  shared_preferences: ^2.2.2
  flutter_secure_storage: ^9.0.0
  
  # Video player
  video_player: ^2.8.2
  chewie: ^1.7.4
  
  # UI enhancements
  cached_network_image: ^3.3.1
  shimmer: ^3.0.0
  flutter_svg: ^2.0.9
  google_fonts: ^6.1.0
  
  # Utils
  intl: ^0.19.0
  url_launcher: ^6.2.2
  connectivity_plus: ^5.0.2

dev_dependencies:
  flutter_test:
    sdk: flutter
  flutter_lints: ^3.0.1

flutter:
  uses-material-design: true
  generate: true

  assets:
    - assets/images/
